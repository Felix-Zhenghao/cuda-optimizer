# 1.3. The CUDA Platform

Describes the CUDA platform software stack: compute capability versioning, NVIDIA Driver, CUDA Toolkit, runtime API vs driver API, PTX virtual ISA, and binary formats (cubins, fatbins). Explains binary compatibility rules across compute capability versions and how PTX enables forward compatibility via JIT compilation at application runtime.
