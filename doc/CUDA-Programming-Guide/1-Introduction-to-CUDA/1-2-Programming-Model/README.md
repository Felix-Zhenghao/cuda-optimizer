# 1.2. Programming Model

Covers the CUDA heterogeneous computing model: host/device separation, kernels, thread blocks, grids, warps, and SIMT execution. Also explains GPU memory hierarchy — global DRAM, on-chip shared memory, register files, caches, and unified memory — and how these map to the SM hardware model including thread block clusters for compute capability 9.0+.
