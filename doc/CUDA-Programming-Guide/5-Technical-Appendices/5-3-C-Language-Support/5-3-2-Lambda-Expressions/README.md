# 5.3 C++ Language Support — Lambda Expressions

Covers CUDA-specific rules for lambda expressions in device code: execution space inference, capture of device variables, use in kernel launches, and extended lambda support. Includes constraints on generic lambdas, `__host__ __device__` lambdas, and unevaluated contexts.
