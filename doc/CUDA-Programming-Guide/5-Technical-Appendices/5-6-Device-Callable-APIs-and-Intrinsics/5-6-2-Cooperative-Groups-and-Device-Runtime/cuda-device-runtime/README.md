# 5.6 Device Callable APIs — CUDA Device Runtime

Reference for the device-side CUDA runtime API used in dynamic parallelism: `cudaMalloc`/`cudaFree` from device, `cudaLaunchKernel`, `cudaDeviceSynchronize`, stream creation/destruction, device property queries, and error handling functions callable from GPU kernels.
