# 5.2. CUDA Environment Variables

Complete reference for CUDA environment variables controlling device enumeration (`CUDA_VISIBLE_DEVICES`), JIT compilation caching, asynchronous execution, work queue connection counts, lazy vs. eager module loading, and error logging. Each entry documents possible values, defaults, and example usage.
