# 5.4 Language Extensions — Warp Functions

Reference for warp-level intrinsics: `__shfl_sync`, `__shfl_up/down/xor_sync` for data exchange within a warp; `__vote_sync`, `__ballot_sync` for predicate voting; and `__reduce_add/min/max_sync` warp reduction functions with mask and type support details.
