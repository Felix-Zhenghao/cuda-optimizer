# 5.4 Language Extensions — Kernel Configuration and Synchronization

Documents the `<<<grid, block, smem, stream>>>` kernel launch configuration syntax and all thread synchronization primitives: `__syncthreads`, `__syncwarp`, `__syncthreads_count/and/or`, `__threadfence`, and cooperative groups barriers with arrive/wait semantics.
