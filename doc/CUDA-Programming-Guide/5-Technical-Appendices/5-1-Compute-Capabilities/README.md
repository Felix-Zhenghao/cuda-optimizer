# 5.1. Compute Capabilities

Reference tables for CUDA compute capabilities covering feature support, SM specifications, memory limits, shared memory sizes, and Tensor Core input data types per architecture (7.x through 12.x). Explains baseline, architecture-specific, and family-specific compiler targets (`compute_NNNa`/`compute_NNNf`) with their compatibility rules.
