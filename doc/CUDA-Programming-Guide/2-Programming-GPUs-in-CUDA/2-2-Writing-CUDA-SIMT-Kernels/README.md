# 2.2. Writing CUDA SIMT Kernels

- **simt-thread-hierarchy-memory-spaces**: SIMT execution model, thread/block/grid hierarchy, and all GPU memory spaces (global, shared, registers, local, constant, distributed shared).
- **memory-performance-atomics-occupancy**: Coalesced global memory access, shared memory bank conflicts, matrix transpose examples, atomics, cooperative groups, and SM occupancy calculation.
