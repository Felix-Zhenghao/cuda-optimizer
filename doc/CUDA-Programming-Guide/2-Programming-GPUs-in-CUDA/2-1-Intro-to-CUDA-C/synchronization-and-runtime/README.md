# 2.1 Intro to CUDA C++ — Synchronization and Runtime

Covers CPU-GPU synchronization using `cudaDeviceSynchronize`, complete vector-addition code listings for both unified and explicit memory management styles, block-level synchronization with `__syncthreads()`, and CUDA runtime initialization behavior including context creation, `cudaSetDevice`, and `cudaDeviceReset` semantics.
