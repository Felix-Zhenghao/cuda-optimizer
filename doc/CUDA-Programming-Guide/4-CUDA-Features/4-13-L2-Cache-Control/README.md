# 4.13. L2 Cache Control

L2 cache control APIs for CUDA devices with compute capability 8.0+ allow marking memory accesses as persisting or streaming. Covers setting aside L2 cache for persisting accesses, access property policies (normal/streaming/persisting), persistence examples, resetting policies, and querying L2 cache properties.
