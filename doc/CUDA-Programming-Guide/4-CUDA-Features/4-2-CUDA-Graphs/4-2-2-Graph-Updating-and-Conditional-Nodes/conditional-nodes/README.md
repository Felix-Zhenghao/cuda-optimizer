# Conditional Graph Nodes

Conditional nodes enable dynamic control flow (IF, WHILE, SWITCH) within CUDA graphs, executing body sub-graphs based on device-evaluated condition values. Covers conditional handles, body graph requirements, nesting rules, and default value semantics for fully GPU-resident iterative and branching workflows.
