# 4.2. CUDA Graphs

- **4-2-1-Graph-Structure-and-Building** — Graph node types, edge data, graph creation via explicit API and stream capture, instantiation, and execution.
- **4-2-2-Graph-Updating-and-Conditional-Nodes** — Updating instantiated graphs (whole graph update, node parameter update) and conditional graph nodes (if/while patterns).
- **4-2-3-Graph-Memory-Device-Launch-User-Objects** — Graph memory nodes, device-side graph launch, graph API usage guidelines, and CUDA user objects for resource lifetime management.
