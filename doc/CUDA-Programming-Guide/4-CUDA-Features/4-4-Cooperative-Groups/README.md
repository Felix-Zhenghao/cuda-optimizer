# 4.4. Cooperative Groups

Cooperative Groups API for organizing collaborating CUDA threads at flexible granularities beyond thread blocks. Covers group handles and member functions, thread block groups, tiled partitions, coalesced groups, grid-level groups, synchronization, collective operations (reduce, scan), async data movement, and multi-GPU grid groups.
