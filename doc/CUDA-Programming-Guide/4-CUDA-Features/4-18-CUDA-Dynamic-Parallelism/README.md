# 4.18. CUDA Dynamic Parallelism

CUDA Dynamic Parallelism enables kernels to launch other kernels without returning to the host, enabling recursive and adaptive algorithms. Covers the execution environment, memory coherence and consistency rules, programming interface (nested launches, streams, events, synchronization), programming guidelines, memory limits, and PTX device-side launch details.
