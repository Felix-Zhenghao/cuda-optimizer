# 4.10. Pipelines

Detailed reference for the cuda::pipeline API enabling staged producer-consumer patterns that overlap async data copies with compute. Covers single-stage and multi-stage pipelines, pipeline scope (thread vs block), pipeline shared state, partitioned pipelines with separate producer/consumer thread groups, and code examples.
