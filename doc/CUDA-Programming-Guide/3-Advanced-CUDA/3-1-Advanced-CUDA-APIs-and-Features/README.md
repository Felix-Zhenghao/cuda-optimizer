# 3.1. Advanced CUDA APIs and Features

Covers host-side CUDA APIs: `cudaLaunchKernelEx` for extended kernel launch configs, thread block cluster launching, stream priorities and explicit/implicit synchronization, Programmatic Dependent Kernel Launch for overlapping dependent kernels, batched memory transfers with `cudaMemcpyBatchAsync`, and performance-relevant environment variables.
