# 3.5. A Tour of CUDA Features

A high-level survey of advanced CUDA features organized by purpose: kernel performance (async barriers, TMA, pipelines, cluster launch control), latency reduction (green contexts, stream-ordered allocation, CUDA graphs, PDL, lazy loading), functionality (EGM, dynamic parallelism), interoperability, and fine-grained control (VMM, driver entry points, error logging).
