# 3.3. The CUDA Driver API

Covers the lower-level CUDA driver API: context management (analogous to processes), dynamic module loading from PTX or cubin, kernel launch parameter passing and alignment, and interoperability between driver and runtime APIs allowing mixed usage in a single application.
