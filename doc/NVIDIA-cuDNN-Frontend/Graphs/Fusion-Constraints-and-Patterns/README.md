# Fusion Constraints and Patterns

Documents operation-specific constraints for runtime fusion engines (matmul, convolutions, pointwise, reduction, resampling). Includes examples of supported fusion patterns and normalization forward/backward specifications for Layer Norm, RMS Norm, and Adaptive Layer Norm.
