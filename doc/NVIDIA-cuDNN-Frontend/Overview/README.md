# Overview

Describes the cuDNN frontend Python and C++ API layers for building computation graphs. Covers the complete workflow: creating graphs, defining tensors and operations, validation, building execution plans, optional autotuning, executing graphs, serialization, and error handling. Includes a full API reference table for all supported operations.
