# FAQs

Answers common cuDNN questions covering mixed precision training with AMP, selecting the fastest convolution kernels via the Graph API's engine configuration system and heuristics, why the first v9.0 API call is slower due to lazy kernel loading, and how to link against the v9.0 split library architecture.
