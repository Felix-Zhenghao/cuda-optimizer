# Dynamic Shapes and Kernel Cache

Explains how to enable dynamic shapes in cuDNN Frontend graphs, use the kernel cache to avoid recompilation across same-topology graphs, and override tensor shapes and strides at execution time without rebuilding the execution plan.
