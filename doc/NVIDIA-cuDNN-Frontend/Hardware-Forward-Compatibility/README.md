# Hardware Forward Compatibility

Explains cuDNN's hardware forward compatibility (added in v9.0.0 with CUDA 12+), which allows cuDNN to work on future GPUs via PTX JIT compilation. Covers configuration via environment variables, recommended heuristic modes (CUDNN_HEUR_MODE_A), supported graph patterns, and known limitations for runtime fusion engines.
