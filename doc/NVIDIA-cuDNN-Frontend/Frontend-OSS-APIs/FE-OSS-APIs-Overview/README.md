# FE-OSS APIs Overview

Documents the experimental Python FE APIs under `python/cudnn`, covering installation, high-level wrapper and class API usage patterns, common parameters, and the compiled-kernel lifecycle (check_support, compile, execute). Lists all currently implemented operations including GEMM fusions, attention, and normalization.
