# Normalizations

Documents cuDNN Frontend normalization operations: batchnorm (forward, finalize, backward), layernorm (forward, backward), adaptive layernorm (forward, backward), instancenorm (forward, backward), and genstats. Each provides C++ and Python APIs with configurable compute data types and running statistics.
