# Pointwise, Reduction, Resampling, Slice

Merged documentation for four cuDNN Frontend operations. Pointwise performs elementwise operations (add, mul, relu, gelu, etc.) between tensors. Reduction reduces tensors along dimensions. Resampling handles pooling and interpolation with configurable modes. Slice extracts tensor sub-regions with Python-style indexing.
