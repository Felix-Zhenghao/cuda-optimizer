# Special-Registers

Merged from: 10-1-Special-Registerstid to 10-14-Special-Registerscluster_ctaid

Documents special registers %tid (thread ID in CTA), %ntid (CTA dimensions), %laneid (lane in warp), %warpid/%nwarpid (warp identifier), %ctaid/%nctaid (CTA in grid), %smid/%nsmid (SM identifier), %gridid (temporal grid ID), %is_explicit_cluster, %clusterid/%nclusterid (cluster in grid), and %cluster_ctaid (CTA in cluster).
