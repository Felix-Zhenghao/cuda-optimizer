# Special-Registers

- **10-1-to-10-14-Special-Registers** — Thread/CTA/grid/cluster identity registers: %tid, %ntid, %laneid, %warpid, %ctaid, %nctaid, %smid, %gridid, %clusterid, %cluster_ctaid.
- **10-15-to-10-33-Special-Registers** — Cluster topology, lane masks, cycle counters, performance monitors, environment registers, global timer, shared memory size registers, and graph execution identifier.
