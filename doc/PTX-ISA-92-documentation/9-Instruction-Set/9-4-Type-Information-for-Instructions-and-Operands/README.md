Defines relaxed type-checking rules for source and destination operands in `ld`, `st`, and `cvt` instructions. Source operands wider than instruction type are truncated (chopped); destination operands wider than instruction type are zero- or sign-extended. Floating-point types require exact size matches unless paired with bit-size types. Reference tables cover all valid type combinations.
