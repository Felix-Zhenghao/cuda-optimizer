PTX video instructions for subword integer operations targeting multimedia workloads.

- **9-7-18-1-Scalar-Video-Instructions**: Scalar operations on byte/half-word/word subword elements including arithmetic (`vadd`, `vsub`, `vabsdiff`, `vmin`, `vmax`, shifts), multiply-accumulate (`vmad`), and comparison (`vset`)
- **9-7-18-2-SIMD-Video-Instructions**: SIMD parallel operations on packed half-words (2-way) and bytes (4-way) for arithmetic and comparison
