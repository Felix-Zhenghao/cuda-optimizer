Introduces Tensor Memory (tcgen05): a dynamically allocated, 32-bit addressed memory with 128 lanes and column-based organization. Covers addressing (lane index + column index), dynamic allocation in units of 32 columns (power of 2, range 32-512), explicit deallocation requirement before kernel exit, and CTA-pair grouping (even/odd CTA pairs sharing Tensor Memory for collective operations).
