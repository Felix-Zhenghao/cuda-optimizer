The `call` instruction transfers execution to a function, saving the return address. Supports direct calls (by name) and indirect calls (via function pointer). Indirect calls require either a complete list of targets (`flist`/`.calltargets`) or a prototype (`fproto`/`.callprototype`). Arguments are pass-by-value. Optional `.uni` marks non-divergent calls. Indirect call requires sm_20.
