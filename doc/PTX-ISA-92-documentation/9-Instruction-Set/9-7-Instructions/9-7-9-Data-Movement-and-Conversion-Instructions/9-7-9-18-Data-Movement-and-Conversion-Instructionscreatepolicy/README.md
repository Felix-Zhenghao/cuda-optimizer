Documents `createpolicy`, which builds a 64-bit opaque cache eviction policy descriptor for use with `.level::cache_hint` on load/store instructions. Supports range-based policies (primary and secondary address ranges) and fraction-based policies (probabilistic priority assignment). Also converts CUDA API access properties via `createpolicy.cvt`. Introduced PTX ISA 7.4; requires `sm_80`+.
