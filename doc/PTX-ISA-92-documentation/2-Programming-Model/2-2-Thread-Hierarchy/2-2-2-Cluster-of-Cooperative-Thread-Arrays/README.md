# 2.2.2. Cluster of Cooperative Thread Arrays

A cluster is a group of CTAs that run concurrently and can synchronize and communicate via shared memory. Clusters support cluster-wide barriers and unique CTA identifiers. Cluster-level features require sm_90 or higher and are optional at launch time.
