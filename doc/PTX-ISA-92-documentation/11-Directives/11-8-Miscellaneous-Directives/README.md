# 11.8.1. Miscellaneous Directives:.blocksareclusters

Documents `.blocksareclusters` directive, which changes the grid launch configuration semantics so that the specified block count is interpreted as a cluster count rather than a thread-block count. Requires `.reqntid` and `.reqnctapercluster` to be also specified on the same `.entry` function.
