# Cluster-Dimension-Directives

Documents `.reqnctapercluster` directive for specifying the required number of CTAs per cluster (1D, 2D, or 3D). Sets cluster configuration that the runtime uses at launch time if not overridden. Mismatched cluster dimension at launch causes a runtime error. Requires sm_90 or higher.
