# 11.3.3. Control Flow Directives:.callprototype

Defines `.callprototype` directive for declaring function prototypes used in indirect calls. Associates a prototype (with optional input/return parameters, .noreturn, .abi_preserve) with a label, enabling type-safe indirect function calls via `call` instruction with a function pointer.
