# 11.2.2. Kernel and Function Directives:.func

Defines `.func` directive for declaring device functions with optional input/return parameters, attributes, and body. Supports optional `.noreturn` (no return to caller), `.attribute` (additional function metadata), and `.abi_preserve`/`.abi_preserve_control` directives for specifying register preservation counts.
