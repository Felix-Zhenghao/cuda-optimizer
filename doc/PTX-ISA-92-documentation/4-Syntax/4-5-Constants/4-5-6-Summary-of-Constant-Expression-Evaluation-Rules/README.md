# 4.5.6. Summary of Constant Expression Evaluation Rules

A reference table summarizing how each PTX constant expression operator maps operand types to result types. Covers primary, unary, cast, binary, and ternary operators for both integer (.s64/.u64) and floating-point (.f64) operands.
