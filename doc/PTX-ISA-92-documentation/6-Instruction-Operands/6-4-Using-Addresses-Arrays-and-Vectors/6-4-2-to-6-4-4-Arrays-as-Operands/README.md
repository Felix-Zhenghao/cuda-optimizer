# 6.4.2–6.4.4. Arrays, Vectors, and Labels as Operands

Arrays are accessed via constant index, register, or register-plus-offset. Vector operands use .x/.y/.z/.w or .r/.g/.b/.a suffixes and support wide loads/stores for better memory throughput. Labels and function names are valid operands only in branch and call instructions.
