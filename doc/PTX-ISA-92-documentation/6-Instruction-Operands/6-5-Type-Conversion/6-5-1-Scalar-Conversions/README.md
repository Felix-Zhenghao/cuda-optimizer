# 6.5.1. Scalar Conversions

The cvt instruction converts between integer, floating-point, and low-precision formats. Narrowing conversions truncate (chop) bits; widening uses sign-extension or zero-extension. Conversion tables cover s8–s64, u8–u64, f16, f32, f64, bf16, tf32, and micro-precision types.
