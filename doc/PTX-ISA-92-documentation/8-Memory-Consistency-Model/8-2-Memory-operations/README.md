# Memory-operations

Defines fundamental memory operation concepts: overlapping locations (same virtual address, partial or complete), aliases (distinct virtual addresses mapping to the same location), multimem addresses (valid only for multimem.* operations), vector/packed types modeled as unordered scalar operations, and per-byte initialization before threads start.
