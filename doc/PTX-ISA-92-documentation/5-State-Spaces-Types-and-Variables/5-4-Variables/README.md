# Variables

- **5-4-1-to-5-4-3-Variable-Declarations** — Variable declaration syntax covering scalars, `.v2`/`.v4` vectors, and multi-dimensional arrays with optional initializers.
- **5-4-4-Initializers** — C-style initializer syntax for constant/global variables including `generic()`, `mask()` operators and function-pointer table initialization.
- **5-4-5-to-5-4-8-Alignment** — Alignment specifier, parameterized variable name shorthand, and `.attribute` directive with `.managed` and `.unified` options.
