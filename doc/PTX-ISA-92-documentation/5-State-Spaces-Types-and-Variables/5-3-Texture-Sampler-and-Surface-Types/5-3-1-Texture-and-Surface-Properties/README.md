# 5.3.1. Texture and Surface Properties

Describes texture and surface dimension fields (`width`, `height`, `depth`) specifying element counts per dimension, plus `channel_data_type` and `channel_order` fields whose enumeration values correspond to the source language API (e.g., OpenCL). These properties distinguish `.texref` and `.surfref` opaque types used in PTX texture instructions.
