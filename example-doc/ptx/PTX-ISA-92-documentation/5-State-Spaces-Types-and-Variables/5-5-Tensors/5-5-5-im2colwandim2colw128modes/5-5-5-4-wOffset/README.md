# 5.5.5.4. wOffset

Explains the wOffset argument for multi-buffer tensor copy operations in im2col::w mode. wOffset adjusts the source pixel location along the W dimension for each shared memory buffer by shifting both bounding box corners and the W coordinate. Includes examples showing multi-buffer loading with different wHalo and wOffset combinations.
