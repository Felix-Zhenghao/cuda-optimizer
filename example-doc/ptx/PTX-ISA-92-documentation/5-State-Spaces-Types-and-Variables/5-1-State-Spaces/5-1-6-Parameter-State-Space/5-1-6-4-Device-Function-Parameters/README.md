# 5.1.6.4. Device Function Parameters

Explains how .param space is used for device function parameters, primarily for passing objects by value that exceed register size (e.g., C structures). Covers flattened byte array passing, ld.param/st.param usage, and address-taking behavior that forces parameters to .local space with stack allocation.
