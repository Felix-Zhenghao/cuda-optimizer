# 5.1.8. Texture State Space (deprecated)

Documents the deprecated .tex state space for read-only, cached global memory accessed via texture instructions. Covers hardware texture binding allocation, 16-byte alignment requirements, and the migration path to .texref variables in .global state space for modern PTX code.
