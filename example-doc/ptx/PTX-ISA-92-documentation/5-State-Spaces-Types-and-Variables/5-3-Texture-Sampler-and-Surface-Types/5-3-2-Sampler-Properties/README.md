# 5.3.2. Sampler Properties

Documents sampler properties for texture access: normalized_coords (coordinate range mode), filter_mode (nearest/linear interpolation), and addr_mode (wrap/mirror/clamp for out-of-range coordinates). Covers independent texture mode with .samplerref/.texref/.surfref variable types, force_unnormalized_coords for OpenCL compatibility, and module-scope initialization syntax.
