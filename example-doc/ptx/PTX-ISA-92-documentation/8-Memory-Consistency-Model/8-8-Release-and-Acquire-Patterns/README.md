# 8.8 Release and Acquire Patterns

Defines release and acquire patterns for memory synchronization. Release patterns make prior operations visible to other threads via release operations or fences followed by strong writes. Acquire patterns make other threads' operations visible via acquire operations or strong reads followed by acquire fences.
