# 2.1. A Highly Multithreaded Coprocessor

Describes the GPU as a massively parallel coprocessor to the host CPU. Data-parallel, compute-intensive application portions are offloaded to the device as kernel functions, compiled to PTX, and translated at install time to the target GPU instruction set.
