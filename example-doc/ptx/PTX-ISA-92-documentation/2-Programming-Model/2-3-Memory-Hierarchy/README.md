# 2.3. Memory Hierarchy

Describes PTX memory state spaces: per-thread local memory, per-CTA shared memory (also accessible cluster-wide on sm_90+), and global memory. Covers additional read-only spaces (constant, param, texture, surface), cache coherency limitations, and host-device memory transfers via DMA.
