# 2.2.3. Grid of Clusters

Describes how clusters of CTAs are batched into grids for large-scale kernel launches. Covers grid-level identifiers (clusterid, nclusterid, ctaid, nctaid, gridid), inter-grid dependencies via CUDA Graphs, and illustrates grid structure with figures.
