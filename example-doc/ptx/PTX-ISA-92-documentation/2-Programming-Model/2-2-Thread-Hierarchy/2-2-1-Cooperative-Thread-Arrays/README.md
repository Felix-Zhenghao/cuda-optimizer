# 2.2.1. Cooperative Thread Arrays

Defines the CTA (Cooperative Thread Array) as a group of threads executing a kernel concurrently. Covers thread identifiers (tid), CTA dimensions (ntid), intra-CTA synchronization barriers, SIMT warp execution, and the WARP_SZ runtime constant.
