# 2.2.2. Cluster of Cooperative Thread Arrays

Explains clusters as groups of CTAs that run concurrently and can synchronize and communicate via shared memory. Covers cluster-level identifiers (cluster_ctaid, cluster_ctarank), explicit vs. implicit cluster launch, and the sm_90+ requirement.
