# 6.1-6.3 Operand Type Information, Source Operands, and Destination Operands

Covers PTX operand type compatibility rules, including silent casting between compatible types. Describes source operands (a, b, c) for ALU instructions in register state space, predicate operands, and destination operand (d) storage in scalar or vector registers.
