# 6.4 Using Addresses, Arrays, and Vectors

Explains generic addressing where memory instructions without a state space use a windowed address model mapping to global, const, local, or shared memory. Covers array element access via byte addresses or bracket notation, vector operand extraction with .x/.y/.z/.w suffixes, and using labels and function names as operands.
