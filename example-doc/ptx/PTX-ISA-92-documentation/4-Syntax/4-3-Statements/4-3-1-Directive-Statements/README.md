# 4.3.1: Directive Statements

Lists all PTX directive keywords (e.g., `.version`, `.target`, `.entry`, `.func`, `.reg`, `.shared`, `.global`, `.param`). Directives begin with a dot to avoid conflicts with user identifiers. Full table of directives provided for reference.
