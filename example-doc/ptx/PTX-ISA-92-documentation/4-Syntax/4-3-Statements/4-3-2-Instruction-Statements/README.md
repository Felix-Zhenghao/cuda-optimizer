# 4.3.2: Instruction Statements

Describes PTX instruction format: opcode followed by comma-separated operands, terminated by semicolon. Covers guard predicates (`@p`, `@!p`) for conditional execution, destination-first operand ordering, and provides a comprehensive table of all reserved instruction keywords (e.g., `add`, `mul`, `ld`, `st`, `bra`, `call`).
