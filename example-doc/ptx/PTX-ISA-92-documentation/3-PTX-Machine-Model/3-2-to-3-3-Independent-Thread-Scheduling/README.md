# 3.2-3.3. Independent Thread Scheduling and On-chip Shared Memory

Covers Volta's Independent Thread Scheduling, which gives each thread its own program counter and call stack for sub-warp divergence and reconvergence. Also describes on-chip memory types per multiprocessor: registers, shared memory (parallel data cache), constant cache, and texture cache.
