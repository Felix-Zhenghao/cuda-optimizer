# 11.7: Cluster Dimension Directives

Documents directives for configuring thread block clusters: .reqnctapercluster (exact CTA count per cluster), .explicitcluster (require explicit cluster dimensions at launch), and .maxclusterrank (maximum CTAs per cluster). All introduced in PTX ISA 7.8, requiring sm_90 or higher.
