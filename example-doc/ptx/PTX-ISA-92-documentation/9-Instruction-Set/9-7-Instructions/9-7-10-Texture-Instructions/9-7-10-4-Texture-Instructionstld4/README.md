# 9.7.10.4. Texture Instructions: tld4

The tld4 instruction fetches the 4-texel bilinear interpolation footprint from a texture. Supports 2d, a2d, cube, and acube geometries with optional sampler, coordinate offset, depth compare, and residency predicate. Returns texels in counter-clockwise order from lower left.
