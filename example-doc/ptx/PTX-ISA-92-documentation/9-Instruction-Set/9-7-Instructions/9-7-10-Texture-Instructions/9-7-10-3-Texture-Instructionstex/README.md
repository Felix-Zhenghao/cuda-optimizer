# 9.7.10.3. Texture Instructions: tex

The tex instruction performs texture memory lookups supporting 1d/2d/3d geometries, texture arrays, cubemaps, multi-sample textures, and mipmaps. It supports unified and independent texturing modes, optional coordinate offsets, depth compare, and resident predicate. Returns 4-element or 2-element vectors.
