# 9.7.11.1. Surface Instructions: suld

The suld instruction loads data from surface memory using coordinate vectors. Supports 1d/2d/3d surfaces and surface arrays (a1d, a2d). Provides unformatted binary loads with configurable out-of-bounds handling (trap, clamp, zero) and optional cache operations. Supports indirect surface access.
