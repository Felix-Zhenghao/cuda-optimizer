# 9.7.14.4.2. Matrix Storage for WMMA

Defines row-major and column-major memory layouts for WMMA matrices. In row-major format, consecutive row elements are contiguous; in column-major, consecutive column elements are contiguous. The non-leading dimension is called the stride. Specifies alignment requirements for matrix base addresses.
