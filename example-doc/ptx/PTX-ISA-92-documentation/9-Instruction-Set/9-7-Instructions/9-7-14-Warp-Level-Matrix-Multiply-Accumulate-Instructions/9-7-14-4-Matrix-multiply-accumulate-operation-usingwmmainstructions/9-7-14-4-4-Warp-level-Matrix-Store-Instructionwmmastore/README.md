# 9.7.14.4.4. Warp-level Matrix Store Instruction: wmma.store

Documents the `wmma.store` instruction that collectively stores a result matrix D from registers to memory across a warp. Supports row/column layouts for all WMMA shapes. Requires 128-bit aligned addresses and synchronized warp execution. Includes syntax, state space qualifiers, and PTX ISA version requirements.
