# 9.7.17. Stack Manipulation Instructions

- **stacksave, stackrestore** -- Save and restore the stack pointer to/from a register for stack state management.
- **alloca** -- Dynamically allocate memory on the stack frame with configurable alignment, returning a local-memory pointer.
