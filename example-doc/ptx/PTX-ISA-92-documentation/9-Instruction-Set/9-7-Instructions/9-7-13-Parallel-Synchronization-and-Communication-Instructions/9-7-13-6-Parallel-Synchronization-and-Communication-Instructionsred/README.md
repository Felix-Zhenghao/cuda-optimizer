# 9.7.13.6. red

Reduction operations on global and shared memory without returning the original value. Supports bitwise (and/or/xor), integer (add/inc/dec/min/max), and floating-point (add) operations. Includes vector types, f16/bf16 support with noftz qualifier, cache hints, and configurable scope/semantics.
