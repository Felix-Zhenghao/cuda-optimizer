# 9.7.13.5. atom

Atomic read-modify-write operations for thread communication. Supports bitwise (and/or/xor/cas/exch), integer (add/inc/dec/min/max), and floating-point (add) operations on global and shared memory. Includes vector types, b128 support, cache hints, and configurable memory semantics with scope qualifiers.
