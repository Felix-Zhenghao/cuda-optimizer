# 9.7.16.1-5. Tensor Memory

Introduces Tensor Memory, a new per-CTA memory type in Blackwell (sm_100+). Covers 32-bit addressing (lane index + column index), capacity constraints, data types, allocation lifetime, and access patterns. Tensor Memory is used as source/destination for 5th-generation TensorCore MMA and data movement operations.
