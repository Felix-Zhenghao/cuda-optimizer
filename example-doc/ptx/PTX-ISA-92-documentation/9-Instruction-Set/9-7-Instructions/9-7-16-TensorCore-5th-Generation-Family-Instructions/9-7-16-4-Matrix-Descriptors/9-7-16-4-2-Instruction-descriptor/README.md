# 9.7.16.4.2. Instruction Descriptor

Describes the 32-bit instruction descriptor for tcgen05 MMA operations. Encodes matrix shapes, types, transpose/negate flags, sparse mode, zero-column mask enable, and other MMA parameters. Exact bit layout depends on the `.kind` qualifier. Lists all field encodings per kind.
