# 9.7.16.10.6. Shared Memory Layout and Swizzling

Defines shared memory layout and swizzling for tcgen05 MMA B matrix. K-major vs non-K-major orientation is set via instruction descriptor transpose bits. Swizzling mode (none, 32B, 64B, 128B) is set in the shared memory descriptor. Lists valid layout combinations per type.
