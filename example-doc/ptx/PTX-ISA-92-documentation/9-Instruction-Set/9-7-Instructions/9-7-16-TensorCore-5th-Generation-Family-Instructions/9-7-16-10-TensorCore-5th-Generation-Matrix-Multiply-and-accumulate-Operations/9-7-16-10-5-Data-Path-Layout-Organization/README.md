# 9.7.16.10.5. Data Path Layout Organization

Defines layout organizations (A through F) for mapping matrix elements to Tensor Memory lanes. Layout depends on M dimension (64 or 256) and element type/size. Includes diagrams showing how matrix rows and columns map to Tensor Memory lane indices and column indices for each layout.
