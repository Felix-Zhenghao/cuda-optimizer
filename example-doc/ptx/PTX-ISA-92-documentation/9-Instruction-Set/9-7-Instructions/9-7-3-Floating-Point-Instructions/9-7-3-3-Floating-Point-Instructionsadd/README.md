# 9.7.3.3 Floating Point Instructions: add

Documents PTX floating-point `add` for f32, f32x2, and f64 types. Supports rounding modes (rn/rz/rm/rp), flush-to-zero for subnormals, and saturation. The f32x2 SIMD variant (PTX 8.6, sm_100+) performs two parallel f32 additions.
