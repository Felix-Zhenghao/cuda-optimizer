# 9.7.12.5. Control Flow Instructions: call

The call instruction performs function calls with optional return values and parameters. Supports direct calls to named functions and indirect calls via function pointers using call tables (.calltargets) or prototypes (.callprototype). The .uni suffix indicates non-divergent calls.
