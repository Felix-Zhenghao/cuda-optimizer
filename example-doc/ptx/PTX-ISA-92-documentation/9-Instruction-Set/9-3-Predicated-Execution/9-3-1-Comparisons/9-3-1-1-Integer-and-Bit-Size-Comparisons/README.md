# 9.3.1.1. Integer and Bit-Size Comparisons

Defines comparison operators for signed integers (eq, ne, lt, le, gt, ge), unsigned integers (eq, ne, lo, ls, hi, hs), and bit-size types (eq, ne only). Ordering comparisons are not defined for bit-size types. Table 22 provides the complete operator mapping across all three type categories.
