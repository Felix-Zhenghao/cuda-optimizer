# 9.3.2. Manipulating Predicates

Describes how predicate values are computed and manipulated using `and`, `or`, `xor`, `not`, and `mov` instructions. There is no direct conversion between predicates and integers; `setp` generates predicates from integers and `selp` converts predicates back to integer values.
